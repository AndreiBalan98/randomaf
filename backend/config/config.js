const CONFIG = {
    BACKEND: {
        HOST: 'localhost',
        PORT: 3001
    },
    FRONTEND: {
        HOST: 'localhost',
        PORT: 8000,
        BASE_URL: 'http://localhost:8000'
    }
};

module.exports = CONFIG;
